apiVersion: v1
kind: Service
metadata:
  name: {{ include "second-chart-test.fullname" . }}
  labels:
    {{- include "second-chart-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "second-chart-test.selectorLabels" . | nindent 4 }}
